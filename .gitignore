node_modules/
dist/
data/
profile/
artifacts/
.worktrees/
*.db
*.db-shm
*.db-wal
.env
.DS_Store

# Private, deployment-specific material. Never committed to the public repository.
_private/
.denylist
gitleaks-report.json

# Design record lives in the private location, not the public repository.
docs/superpowers/

# Local tool caches. DESIGN.md and PRODUCT.md are deliberate, committed
# artefacts; this directory is the tool's own working state and would be swept
# into a public repository by the first `git add -A` that ran near it.
.impeccable/

# Subagent-driven-development scratch: ledger, briefs, review packages.
# The git history is the record; this is working state for one plan.
.superpowers/
